# 复制为 .env 并填入真实值（.env 已被 .gitignore 忽略，不会提交）

# ─── OpenRouter API Key（必填）───
# 在 https://openrouter.ai/keys 获取
OPENROUTER_API_KEY=sk-or-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ─── 模型选择（可选）───
# 不改代码即可切换模型，未配置时默认 anthropic/claude-opus-4.8
# 常用可选（均已实测本区域可调通）：
#   anthropic/claude-opus-4.8        最强，走 Anthropic
#   anthropic/claude-opus-4.8-fast   opus-4.8 快速版
#   anthropic/claude-opus-4.5        走 Amazon Bedrock
#   anthropic/claude-sonnet-4.5      性价比高，走 Amazon Bedrock
# OPENROUTER_MODEL=anthropic/claude-opus-4.8

# ─── 代理设置（可选）───
# Node.js 内置 fetch 不会自动读取系统代理。若你在有区域限制的网络环境下
# 访问 OpenRouter/Claude，需要让请求走本地代理（如 Clash 默认 7890 端口）。
# 程序会优先读取系统环境变量 HTTPS_PROXY；若系统已设置则此处可留空。
# 仅当系统未设置、或想为本项目单独指定时，取消下一行注释并填入：
# HTTPS_PROXY=http://127.0.0.1:7890

# ─── 服务端口（可选）───
# SSR 生产服务器(npm run serve:ssr:app)监听端口，默认 4000；
# 开发服务器(npm start / ng serve)默认 4200。
# PORT=4000

# ─── 响应缓存 Redis（可选）───
# chatFlow 内置了缓存层来降低模型调用：
#   ① 模板热点（注册/登录等固定 UI）：直接命中预建模板，0 调模型，跨用户复用；
#   ② 通用响应缓存：仅对单轮请求(clearSession=true)按 归一化输入+模型+版本 的
#      SHA-256 做 key 缓存，多轮对话不缓存以免串话。
# 配置 REDIS_URL 即用 Redis（跨进程/实例共享，适合生产）；
# 不配置或连不上时，自动降级为进程内 LRU 缓存（零依赖，clone 后即可直接跑）。
# REDIS_URL=redis://127.0.0.1:6379
